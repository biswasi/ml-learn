# ml-learn
1.Create a new repository on the command line
 a. echo "# ml-learn" >> README.md
 b .git init
 c. git add README.md
 d. git commit -m "first commit"
 e .git branch -M main
 f .git remote add origin git@github.com:biswasi/ml-learn.git
 g. git push -u origin main

----------------------------------------------------------------------------------------------------------------------

ML algorithim based on following use cases.

•	Grid -search.
•	One-hot encoding , (MICE) SMOTE ,Compare multiple algorithms ,(RFE)
•	Logistic regression.
•	Association rule.
•	Linear Regression.
•	K-means Clustering

